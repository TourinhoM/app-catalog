{
  "name": "app-catalog",
  "version": "1.0.0",
  "description": "CRUD de itens — referência de plataforma engineering",
  "main": "src/index.js",
  "scripts": {
    "start": "node --require @opentelemetry/auto-instrumentations-node/register src/index.js",
    "dev": "node --require @opentelemetry/auto-instrumentations-node/register --watch src/index.js"
  },
  "dependencies": {
    "@opentelemetry/auto-instrumentations-node": "^0.56.0",
    "fastify": "^4.29.0",
    "pg": "^8.13.0",
    "prom-client": "^15.1.3"
  }
}
